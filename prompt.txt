**AGENT 1 DEPLOYMENT: Component & UI Type Fixer - SPECIFIC ERRORS**

I've identified the exact Component & UI TypeScript errors you need to fix:

**CRITICAL ERRORS TO FIX:**

1. **subscription-card.test.tsx:94** - Mock function type mismatch
   ```
   Argument of type 'Mock<(options: any) => { mutate: Mock<Procedure>; mutateAsync: Mock<() => Promise<{ id: string; status: string; }>>; isLoading: boolean; }>' is not assignable to parameter of type 'NormalizedProcedure<() => { mutate: Mock<Procedure>; isLoading: boolean; }>'.
   ```

2. **premium-feature-gate.tsx:36 & 96** - Action type mismatch  
   ```
   Type 'string' is not assignable to type '"export_data" | "add_bank_account" | "invite_team_member" | "use_ai_assistant"'.
   ```

3. **cancellation-monitoring-dashboard.tsx:110** - tRPC option type mismatch
   ```
   'onSuccess' does not exist in type 'DefinedUseTRPCQueryOptions'
   ```

4. **cancellation-monitoring-dashboard.tsx:576** - Complex analytics type conversion error

**YOUR MISSION:**
1. Fix the mock function types in subscription-card.test.tsx
2. Fix the action string literal types in premium-feature-gate.tsx  
3. Fix the tRPC query options in cancellation-monitoring-dashboard.tsx
4. Fix component prop type mismatches

**IMMEDIATE ACTION REQUIRED:**
Start with premium-feature-gate.tsx - the action string needs to be properly typed as a literal union type instead of generic string. Then fix the test mock function signatures.

These are blocking CI/CD - fix them now!