# Docker Production Environment Configuration
# This file is used when building production Docker images

# Build-time environment validation
SKIP_ENV_VALIDATION="true"

# Production defaults (override at runtime)
NODE_ENV="production"
NEXTAUTH_URL="https://your-domain.com"

# Note: All sensitive values should be provided at runtime
# via Docker environment variables or secrets management